<UserControl x:Class="GrapheneSensore.Controls.HeatMapControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="450">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        
        <TextBlock Grid.Row="0" 
                   Text="Pressure Heat Map" 
                   FontSize="16" 
                   FontWeight="Bold"
                   Margin="10"
                   HorizontalAlignment="Center"/>

        
        <Border Grid.Row="1" 
                BorderBrush="#CCCCCC" 
                BorderThickness="1"
                Margin="10">
            <Canvas x:Name="HeatMapCanvas" 
                    Background="White"
                    SizeChanged="HeatMapCanvas_SizeChanged"/>
        </Border>

        
        <Grid Grid.Row="2" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Low Pressure" VerticalAlignment="Center"/>
            
            <Rectangle Grid.Column="1" Height="20" Margin="10,0">
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#0000FF" Offset="0.0"/>
                        <GradientStop Color="#00FFFF" Offset="0.25"/>
                        <GradientStop Color="#00FF00" Offset="0.5"/>
                        <GradientStop Color="#FFFF00" Offset="0.75"/>
                        <GradientStop Color="#FF0000" Offset="1.0"/>
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>

            <TextBlock Grid.Column="2" Text="High Pressure" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</UserControl>
