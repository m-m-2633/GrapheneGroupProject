<Window x:Class="GrapheneSensore.Views.ApplicantDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Applicant Information" 
        Height="550" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <Window.Resources>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#424242"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#2196F3"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="25,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SaveButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#388E3C"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CancelButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#757575"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#616161"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        
        <Border Grid.Row="0" Background="#1976D2" Padding="20,15">
            <StackPanel>
                <TextBlock Text="👤 Applicant Information" 
                           FontSize="20" 
                           FontWeight="Bold" 
                           Foreground="White"/>
                <TextBlock x:Name="SubtitleText" 
                           Text="Enter applicant details below" 
                           FontSize="12" 
                           Foreground="#E3F2FD" 
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="30,25">
                
                <TextBlock Text="First Name *" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="FirstNameTextBox" Style="{StaticResource TextBoxStyle}"/>

                
                <TextBlock Text="Last Name *" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="LastNameTextBox" Style="{StaticResource TextBoxStyle}"/>

                
                <TextBlock Text="Email Address" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="EmailTextBox" Style="{StaticResource TextBoxStyle}"/>

                
                <TextBlock Text="Reference Number" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="ReferenceNumberTextBox" Style="{StaticResource TextBoxStyle}"/>

                
                <TextBlock Text="Notes" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="NotesTextBox" 
                         Style="{StaticResource TextBoxStyle}"
                         TextWrapping="Wrap" 
                         AcceptsReturn="True"
                         Height="100"
                         VerticalScrollBarVisibility="Auto"/>

                <TextBlock Text="* Required fields" 
                           FontSize="12" 
                           Foreground="#F44336" 
                           FontStyle="Italic" 
                           Margin="0,5,0,0"/>
            </StackPanel>
        </ScrollViewer>

        
        <Border Grid.Row="2" Background="White" Padding="20,15" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CancelButton" 
                        Content="✕ Cancel" 
                        Click="CancelButton_Click" 
                        Style="{StaticResource CancelButtonStyle}" 
                        Margin="0,0,10,0"/>
                <Button x:Name="SaveButton" 
                        Content="✓ Save Applicant" 
                        Click="SaveButton_Click" 
                        Style="{StaticResource SaveButtonStyle}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
